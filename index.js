

(prompt)("Input a Farenheit temperature value and this will convert it to Celsius");

function convertFarenheittoCelsius(num) {
    let celsius = prompt ("Input a Farenheit temperature and this will convert it to Celsius:");
    const fahrenheit: number = ((celsius - 32) * (5/9));

    alert(`${celsius} is ${fahrenheit} This is your temperature in Celsius: ${fahrenheit}`);
}

 
// const tempNumberInput = prompt(`Input your temperature`);

// const (convertToCelcius): "this is your temperature in celsius"

// // (tempNumberInput - 32) * 5/9};

// function name (convertToCelcius);

// let temp = 
// const convertToCelcius = (`this is your temperature in celsius`);



//prompt the user to input a f temp and let them know we will convert to Celsius
// create a function that called convertToCelsius  that takes in the f input and returns the Celsius



//let num2 = (prompt("your temperature in Celsius is"));

//const num2 = 

